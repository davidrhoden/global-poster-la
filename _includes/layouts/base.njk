<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">

	{% include "components/head.njk" %}

	<body class="schibsted-grotesk-500">

		<header style="text-align: center">
			<em>
				<a href="{{ '/' | url }}"><img src="/static/img/global-poster-logo.svg" alt="{{ metadata.title }}" style="max-width: 500px;"></a>
			</em>
			{% include "components/nav.njk" %}
		</header>

			{{ layoutContent | safe }}

		{% include "components/footer.njk" %}

	</body>

</html>
